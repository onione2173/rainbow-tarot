# 인수인계 — 일본 매출 미션 (다른 터미널/세션에서 이어가기)

이 파일을 새 세션의 클로드 코드에 "HANDOFF.md 읽고 이어서 진행해줘"라고 붙여넣으면 됩니다.
(작업 디렉토리는 `~/rainbow-tarot`, 브랜치는 보통 `dev`에서 작업 후 `main`으로 머지.)

세션이 바뀌면 대화 맥락은 안 이어지지만, **메모리(`~/.claude/projects/.../memory/`)에 저장된 결정·피드백은 자동으로 불러와집니다.** 이 파일은 그걸 보완하는 상세 스냅샷입니다.

---

## 현재 미션

**한 달 내(2026-08-22까지) 일본에서 신규 매출 ₩100,000 이상.**

- 이전엔 "팔로워 100명까지는 판매 언급 자제"였는데 2026-07-22에 폐기함 — 사용자가 "너무 소극적인 액션만 하고 있는듯"이라고 직접 지적. 지금은 CTA를 정기적으로 노출하고, 오가닉 소통량도 적극적으로 늘리는 방향.
- 광고비: **아직 사용 안 하기로 함**(2026-07-24 기준). 다시 물어봐야 할 수도 있음.

## 팀 구조 ("직원" 컨셉 — 사용자가 원한 방식)

- **팀장 = Fable 5 (나, 메인 세션)** — 전체 총괄, 외부 게시/결제 변경 등 리스크 있는 액션은 항상 여기서 승인 게이트 거침. 매일 `log/team-lead-journal.md`에 일지 씀(오늘 한 일 / 오늘 틀린 일, 윤자동 스레드 톤 — 딱딱한 보고체 아니고 "~다" 짧은 문장체).
- **마케팅1 (콘텐츠 제작/포스팅)** — Agent 툴, subagent_type `fast-worker`. 페르소나 확립 초반엔 model `opus`, 안정화 후 `sonnet`. 지금까지: 후쿠마루 페르소나 1주차 콘텐츠 21개(JP+KR) 초안 작성, 이모지→가오모지 교체, 가격 배지 UI 롤아웃 등을 위임했었음.
- **마케팅2 (댓글 대응)** — 아직 실제로 가동 안 함(설계만 돼있음). model `haiku` 기본, 애매하거나 부정적인 댓글은 sonnet/팀장으로 에스컬레이션.
- 모든 콘텐츠/댓글은 **일본 감수성 체크리스트**(`marketing/persona-guide.md` 참고: 과장광고 금지, 펫로스 톤 주의, 감정 콘텐츠 직후 하드셀 금지, 마스코트 상표 충돌 확인, 문화적 금기)를 통과해야 함.

## 캐릭터: 후쿠마루 (ふくまる)

- 검은고양이 컨셉(福=복), 츤데레 성격, 말투는 にゃ/にゃん 어미(한국식 "냥냥(대사)" 형식 안 씀), 1인칭 僕(ぼく).
- **프로필 사진은 실제로는 흰 고양이**(로브 입고 타로 보는 일러스트) — "로브를 입은 순간만 검은고양이 복을 빌린다"는 설정으로 정합성 맞춤.
- 이모지 최소화, 가오모지(顔文字) 사용: (´˘\`) ฅ^•ω•^ฅ (ﾉωﾉ) ( ˘ω˘) (-ω-。) (｡•ᴗ•｡)✧ — 유니코드 이모지 많이 쓰면 AI 티 난다는 사용자 피드백 반영.
- 답글/소통 시엔 이모지도 최소, 「」/"" 따옴표 인용 문체도 쓰지 않음(부자연스러움) — 짧고 진짜 반응처럼.

## X 계정 현황

- 핸들: **@fukumaru_tarot** (사용자 개인 계정 @onione2173과 별개, 새로 만든 계정)
- 콘텐츠 큐: `marketing/fukumaru-queue-ja.md` — 21개 중 1~6번 게시 완료(날짜/URL 기록돼있음), 22번은 예비 FAQ(수동 트리거 전용, 자동 게시 로직에서 제외)
- 팔로잉 20+개 계정(라이브 해시태그 검색으로 신선한 글 찾아서 팔로우+좋아요+짧은 댓글 하는 방식이 효과적이었음 — `#猫のいる暮らし` `#うちの子自慢` `#猫好きさんと繋がりたい` 등)
- 팔로워는 아직 거의 없음(1명, 그나마 콘텐츠 보고 온 건 아닌 걸로 보임) — 정직하게 느린 상태
- 경쟁 계정 조사함: **@nekoyomi_yamaz** — 고양이 탐정 부부가 코코나라에서 타로 리딩 파는 계정. 46팔로워로도 실제 12건 판매(₩14,000 상당) 달성 — 팔로워 수보다 해시태그 도달+신뢰신호(리뷰)가 더 중요하다는 시사점.
- **코코나라 진출은 포기함**: 일본 전화번호·신분증·은행계좌 필요해서 한국 거주자는 정산(현금화)이 사실상 안 됨. 대신 우리 사이트 직결제 채널에 집중.

## 자동 게시 파이프라인 (GitHub Actions — 세션 없이도 작동)

- `.github/workflows/fukumaru-post.yml` — 매시간 실행, `main` 브랜치 기준으로만 스케줄 트리거됨(그래서 dev 작업 후 반드시 main으로 머지+push 필요)
- `scripts/fukumaru/` — post_next.py(진입점), schedule_state.py(하루 랜덤 시각 3~5개 관리, 상태는 `marketing/state/fukumaru-schedule.json`에 커밋), x_client.py(X API v2 OAuth1.0a), gemini_image.py(이미지 생성, 참조 이미지로 캐릭터 일관성 유지), queue_parser.py
- GitHub Secrets 등록 완료: `GEMINI_API_KEY`(네오라 프로젝트 키 재사용), `X_API_KEY`, `X_API_SECRET`, `X_ACCESS_TOKEN`, `X_ACCESS_TOKEN_SECRET` — 전부 실제 호출로 인증 확인함(200 OK)
- **2026-07-22/23에 실제로 라이브 확인 완료** — 세션 없이 GitHub 서버에서 스스로 스케줄 판단하고 상태 파일 커밋하는 것까지 확인함. 더 이상 이론이 아니라 실제로 돌아가는 상태.
- X 앱은 "Pay Per Use" 요금제, 현재 잔액 $0/결제수단 없음/지출한도 무제한 — 카드 없어서 지금은 과금 자체가 안 되는 구조. 사용자가 지출한도 설정은 나중으로 미룸.
- Gemini 이미지 생성은 네오라 봇이랑 키 공유해서 **할당량(429) 이슈가 있을 수 있음** — 실패해도 텍스트만으로 폴백되게 설계돼있음.

## 가격 (2026-07-24 변경)

"오픈 이벤트 50% 할인" 프레임으로 전부 통일:
| 시장 | 정가(취소선 표시) | 실제가 |
|---|---|---|
| 한국 | ~~₩3,000~~ | ₩1,500 (안 바뀜) |
| 일본 | ~~¥600~~ | **¥300** (¥400에서 인하 — 결제 검증 로직도 동기화함) |
| 영어권 | ~~$5.99~~ | $2.99 (안 바뀜) |

일본 가격 변경은 `ja/deep-reading/index.html` + `netlify/functions/paypal-order.js` + `netlify/functions/deep-tarot-background.js` 세 곳이 정확히 일치해야 결제가 안 깨짐(과거 "캡처 금액 vs 원 주문 금액 불일치" 사고 있었음, 재발 주의).

## 계획됐지만 아직 구현 안 한 것

- **후기 제출 기능**: 결제 완료 후 결과 페이지에서 별점(필수)+자유 텍스트(선택, "혹시 더 하고싶은 말씀이...") 받고, `approved=false`로 저장 → 사람이 검수 후 공개. **가짜 후기는 절대 금지**(경품표시법 리스크 + 브랜드 신뢰 붕괴 위험) — 실제 후기 쌓일 때까지 위젯은 비워두거나 숨김. Supabase `reviews` 테이블 설계는 돼있음(위 대화 참고), 아직 코드로 안 옮김.

## 알아둬야 할 함정들

- **X 신규 계정 "Unlock more on X" 게이트**: 초반엔 검색 노출·DM 등이 제한됨, 시간 지나야 풀림.
- **280자 제한 + 이모지 가중치**: 결합 이모지(🐈‍⬛ 등)가 일반 글자보다 많이 카운트됨. 게시 직전 카운터 확인 습관.
- **X 웹 Post 버튼이 가끔 두 번 눌러야 반영됨** — 클릭 후 스크린샷으로 실제 게시됐는지("Your post was sent" 토스트/포스트 개수 증가) 꼭 확인.
- **file_upload 브라우저 툴이 로컬 파일 경로를 못 받는 버전 이슈** — 이미지 업로드는 사용자가 직접 해야 함.
- **`gh` 워크플로 파일 push엔 `workflow` OAuth 스코프 필요** — 없으면 `gh auth refresh -h github.com -s workflow` (사용자가 device code 승인 필요) 후 `gh auth setup-git` 까지 해야 실제로 반영됨.
- **세션 크론(CronCreate)/백그라운드 서브에이전트는 터미널 끄면 같이 죽음** — GitHub Actions 파이프라인만 완전히 독립적. 새 세션 열 때마다 "오늘 크론 걸렸는지"부터 확인하는 습관 필요(단, 이제 GitHub Actions가 있어서 예전만큼 중요하지 않음).
- **push는 항상 사용자 확인 받고 진행** — 로컬 커밋까지는 알아서 해도 되지만 push/머지는 매번 여쭤봄.

## 🔧 제품 변경 로그 (2026-08-12)

- **"무지개다리 너머" → "무지개다리 그 후"로 개명 완료.** 동명 펫타로 저자의 책 제목과 겹쳐 오해 소지가 있다는 사용자 판단(저작권 분쟁 예방). ko/ja(虹の橋のあと)/en(After the Rainbow Bridge) 전체 페이지(9개 파일) + 마케팅 미게시 큐 텍스트(`x-queue-ja.md`, `fukumaru-queue-ja.md`) 반영. URL 슬러그(`/rainbow-tarot/`)는 이미 영문이라 안 바꿈 — SEO 영향 없음.
- **딥리딩에 "같은 유저+같은 펫의 과거 이별 신호 감지" 기능 추가** (`netlify/functions/deep-tarot-background.js`): 같은 user_id+pet_name의 과거 완료된 딥리딩에서 이별/사망 키워드가 감지되면, 새 리포트 프롬프트에 "톤·시제만 자연스럽게 맞추고 절대 인용/출처 언급 금지" 내부 지시를 얹음. rainbow-tarot(무지개다리 그 후) 페이지는 아직 Supabase 기록을 안 남겨서 이 체크 대상 아님.
- **신규 회원가입 Slack 알림 추가** (`netlify/functions/notify-signup.js` + `assets/auth.js`): 가입 직후 첫 로그인만 감지해 매출 알림과 별개로 Slack에 "🎉 신규 회원가입" 메시지 전송. **Netlify 환경변수에 `SLACK_WEBHOOK_URL` 등록해야 작동**(현재 GitHub Actions 시크릿에만 있고 Netlify엔 없음 — 사용자 액션 필요, 미완료).
- **로그인 GA 이벤트 provider 오표기 버그 수정**: 기존엔 구글 로그인해도 `method:'kakao'`로 잘못 찍혔음 → 실제 provider로 정확히 기록되게 수정.
- **대기(미구현, 방향만 합의)**: "무지개다리 그 후" 결과를 "다시 보려면 로그인" 게이트로 걸어서 회원전환 늘리기 — `deep-reading`의 `linkGoogleIdentity` 익명→계정 연결 패턴 재사용 예정, 아직 착수 안 함.
- **전생 타로 / 케미 타로 신규 추가** (별도 세션, 2026-08-11~16): 무료 원카드 + `deep-reading`의 심층 3카드 확장(`?spread=pastlife`/`chemistry`)으로 ko/ja/en 전부 구현. 프리뷰 텍스트가 유료 리포트 카드1로 그대로 재사용되도록 수정(과거엔 "미리보기에서 보신 것처럼"이라고만 언급해서 헷갈림). 홈 배너 순서/업셀 CTA 2줄 레이아웃/브레드크럼 헤더/카톡 공유 로그인 유도 등 UX 피드백 반영 완료.

## 🔧 제품 변경 로그 (2026-08-19)

- **홈 필터 탭 개편**: "전체/간단한 분석/심층분석" → "전체/원카드/스프레드"로 변경 (ko/ja/en `index.html` 3개 전부). `data-filter`/`data-category` 값도 `simple`/`deep` → `onecard`/`spread`로 리네이밍. 뱃지 텍스트(원카드/스프레드)와 이미 1:1 대응이라 로직 변경 없이 라벨+속성값만 교체.
- **`/deep-reading/`(5개 스프레드가 한 페이지에 뭉쳐있던 심층 콤보 분석) 항목별 분리, 1차분**: 독립 슬러그 3개 신설, 결제는 통일 유지(가격 개별화는 아직 안 함 — 다음 단계).
  - `/past-life-tarot/deep/` — 전생 연대기 스프레드만 (SPREADS 1개, 카테고리 선택 스킵하고 바로 폼으로)
  - `/chemistry-tarot/deep/` — 케미 스프레드만 (동일 패턴)
  - `/guardian-tarot/deep/` — 아이 생각/행동 이상/변화 3개 스프레드 허브 (기존 5개 선택지 중 pastlife/chemistry만 제거, 나머지 그대로 픽커 유지) — mind/behavior/change는 자체 원카드 페이지가 없어서 "걱정될 때" 테마인 guardian-tarot 밑으로 묶음
  - `/deep-reading/` 원본 파일은 완전히 그대로 둠(레거시 허브로 계속 작동 — 기존 결제/재개 플로우, `_returnPath` 없는 오래된 링크 호환용)
  - 관련 페이지 업셀 링크 갱신: `past-life-tarot`→자기 deep, `chemistry-tarot`→자기 deep, `guardian-tarot`/`mood-reading`/`rainbow-tarot`→`guardian-tarot/deep/` (rainbow-tarot 업셀은 원래도 "곁에 남은 아이" 쪽으로 유도하는 카피라 걱정 계열과 자연스럽게 매칭)
  - 홈 배너의 "심층 콤보 분석" 카드도 `/guardian-tarot/deep/`로 링크 변경(카피/이미지는 그대로, 아직 새 아트워크 없음)
  - 로그인 중간 이탈 시 복귀 경로(`_returnPath`)도 각 신규 페이지 자기 자신으로 정확히 세팅됨. 홈페이지의 "카톡인앱→홈으로 튕김" 폴백(`index.html`)도 `_returnPath` 우선 사용하도록 수정.
  - sitemap.xml에 신규 3개 URL 추가.
  - **백엔드(`nicepay-confirm.js`, `deep-tarot-background.js`) 전혀 안 건드림** — spreadKey 값이 그대로라 결제 검증 로직 변경 불필요.
  - 서브에이전트(fast-worker) 위임 후 직접 diff/구조 검증 완료.
- **아직 안 한 것 (다음 단계 후보)**:
  - 무지개다리 그 후(rainbow-tarot)에 죽은 아이 본인에 대한 심층분석 신설 — 콘텐츠(카드 구성/카피)는 아직 미정, 애도 감정 이용 안 하는 톤으로 신중하게 설계 필요. 착수 안 함.
  - 5개(+신설 시 6개) 스프레드 **개별 가격 책정** — 아직 전부 동일가(₩1,500). 서버 가격 검증 로직(`EXPECTED_AMOUNT` 단일 상수)을 스프레드별로 바꿔야 하는 작업이라 별도 세션 필요.
  - ja/en 버전은 이번 분리 작업 범위 밖(건드리지 않음).
  - mypage의 "이어서 결제" 재개 링크는 여전히 `/deep-reading/`로 감(기능은 정상 작동, 신규 슬러그로 안 바꿈 — 우선순위 낮음).

## 🔧 제품 변경 로그 (2026-08-22) — 심층 콤보 분석 완전 분리 2차

- 사용자 피드백으로 `/guardian-tarot/deep/`(mind/behavior/change 3옵션 허브) 방식을 접고, 셋 다 `/mind-tarot/`, `/behavior-tarot/`, `/change-tarot/`로 완전 독립 페이지화. 이제 5개 스프레드(전생/케미/마음/행동/변화)가 전부 각자 슬러그를 가짐. `/deep-reading/`(5개 통합 원본)과 `/guardian-tarot/deep/`(3옵션 허브)는 파일만 남기고 홈/원카드 페이지에서 안 씀 — 레거시 링크·재개 플로우 호환용.
- `guardian-tarot`("우리 아이 걱정될 때") 업셀을 카드 1개 → 3개 연달아 표시로 변경, 각각 mind/behavior/change 페이지로 직결.
- `mood-reading`/`rainbow-tarot` 업셀은 여전히 `/guardian-tarot/deep/`(3옵션 허브)로 감 — 셋 중 하나를 임의로 고르기 애매해서 허브를 그대로 재활용.
- 각 신규 페이지의 "돌아가기"를 referrer 기반으로 바꿈(같은 사이트에서 왔으면 브라우저 히스토리 back, 아니면 `/guardian-tarot/`로 폴백) — 진입 경로가 여러 개(홈 배너 직접 vs 업셀 경유)라 고정 링크로는 안 맞는 문제 해결.
- 홈 배너에서 "심층 콤보 분석" 카드 제거, 대신 스프레드 5개(전생 연대기/케미/마음/행동/변화) 전부 각자 카드로 노출 — 원카드 카드 바로 뒤에 배치, 이미지는 신규 아트워크 없이 기존 이미지 재사용.
- 가격은 이번에도 전부 그대로(₩1,500), 백엔드 안 건드림.

## 🔧 제품 변경 로그 (2026-08-22) — 브랜드명 확장

- 한국어 페이지 브랜드를 "펫타로" → "달의 서재 펫타로"로 확장. "달의 서재"는 사용자가 당근마켓 사주 서비스에서 쓰는 개인 브랜드 — 신뢰 시그널로 얹은 것, 전면 리브랜딩 아님.
- 타이틀/og:title 패턴: `달의 서재 | {카테고리} 펫타로 {원카드|스프레드}` (예: "달의 서재 | 전생 펫타로 원카드" vs "…스프레드" — 같은 주제의 무료/유료 페이지 구분에 홈 필터 탭이랑 같은 원카드/스프레드 용어 재사용). 기능 페이지(마이페이지/문의하기/약관/개인정보/404)는 `달의 서재 | 마이페이지` 식으로 심플하게.
- 헤더 로고 텍스트도 동일하게 변경. 브레드크럼 헤더(로고 | 현재페이지명) 쓰는 12개 페이지는 텍스트가 3배 길어져서 모바일(580px 이하)에서 페이지명 부분을 숨기는 미디어쿼리 추가 — 헤더가 고정 높이 sticky라 두 줄 wrap은 리스크 커서 피함.
- 카톡 공유 카드 워터마크("🐾 펫타로")도 동일 변경.
- **의도적으로 안 건드림**: ja/en(별도 로컬 브랜드명), auth-callback(과도기 페이지), 약관/마이페이지/문의하기의 본문 산문 속 "펫타로" 언급, pricing/index.html은 브랜드 토큰만 최소 교체(결제사 참조용 영문 페이지로 보여서 구조 유지).

## 대기 중 (막힌 것 아님, 미션 외)

- 해외 결제 수단 확장: Lemon Squeezy 답변 대기(Paddle은 점술 카테고리 거절). 현재 PayPal(¥400)은 살아 있음.
- 한국어 나이스페이 실결제 셀프 테스트(₩1,500) — 우선순위 낮음.
- Supabase RLS 때문에 anon 세션에서 `reading_history` 조회 불가 → 결제 0건이 진짜 0건인지 서버 함수 경유로 확인 필요.

## 운영 규칙 (요약 — 상세는 메모리)

- **git push는 사용자 요청 시에만.** 로컬 커밋까지만 하고 push는 사용자에게 맡길 것.
- Netlify 배포 크레딧 아끼기 — 변경사항 모아서 한 번에.
- 일본어 텍스트는 항상 한국어 번역 병기 (사용자가 일본어 못 읽음).
- 유니코드 이모지 최소화, 가오모지 사용. 답글은 짧고 평범하게(따옴표 스타일 금지).
- 일지는 윤자동식 "~다" 건조체.
- 톤/카피 같은 주관적 결정은 객관식으로 좁히기 전에 사용자가 자유롭게 말할 여지부터 줄 것.
- **브라우저 자동화(Claude in Chrome) 사용량 최소화 원칙 (사용자 지시 7/25): 효과·정확도를 유지하는 선에서 항상 사용량이 가장 적은 방식을 먼저 고른다.**
  - 스크린샷(이미지 토큰)보다 `get_page_text`/`read_page`/`find`(텍스트) 우선. 스크린샷은 시각 확인이 진짜 필요할 때만(레이아웃 확인, 클릭 좌표 잡기 등).
  - 액션을 개별 호출로 쪼개지 말고 batch로 묶어서 왕복 줄이기.
  - 게시·소통 세션은 짧게 끊기 — 스크린샷이 쌓일수록 컨텍스트·비용이 커짐.
  - **단, 결과를 확실히 확인해야 하는 지점(게시 성공 여부, 폼 제출 결과)에서 확인을 생략해 정확도를 깎지 않는다** — 최소화가 검증 스킵으로 이어지면 안 됨.

## 📣 프로모션 플랜 (2026-07-27, Opus 기획)

상세 실행 플랜은 **`marketing/promotion-plan.md`** — 행동 고민 집사 타겟 소통 플레이북(상황별 답글 템플릿 7종), 신규 콘텐츠 10개, 퍼널 수학, 26일 주차별 시퀀스, 정직한 판정. **다음 세션은 이 파일을 소통·콘텐츠의 기준 문서로 삼을 것.** 핵심 판정: ₩100k by 8/22 확률 10~20%(도달이 병목, 노력 문제 아님) → 8/22를 "매출 마감"이 아니라 "소통 엔진 검증 마감"으로 재정의 권고.

## 파일 위치 정리

- 페르소나 가이드: `marketing/persona-guide.md`
- 콘텐츠 큐: `marketing/fukumaru-queue-ja.md`
- 팀장 일지: `log/team-lead-journal.md`
- 일일 운영 보고(방문자/전환): `log/YYYY-MM-DD.md`
- 자동 게시 파이프라인: `scripts/fukumaru/`, `.github/workflows/fukumaru-post.yml`, 상태 파일 `marketing/state/fukumaru-schedule.json`
- 이 인수인계 파일: `~/rainbow-tarot/HANDOFF.md` (진행되는 대로 계속 갱신할 것)

## 매일 보고 형식
`방문자 수 / 유입 채널 / 결제 시도 / 결제 완료 / 오늘 한 일 / 내일 할 일 / 필요한 결정`
