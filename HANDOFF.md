# 인수인계 — 후쿠마루(일본 X) 미션

새 세션에서 "HANDOFF.md 읽고 이어서 진행해줘"로 시작하면 됩니다.
**큰 변화가 생길 때마다 이 파일을 갱신할 것. 할 일 목록은 반드시 여기 기록하고, 기록된 루틴은 반드시 실행할 것 (사용자 지시 7/25).** (최종 업데이트: 2026-07-27 05:00 KST)

## 미션 (변경 이력 있음)

- ~~원래: 7일 내 해외(영/일) 결제 1건~~ (`~/Downloads/rainbow-tarot-mission-prompt.md`)
- **현재: 1개월 내 일본 매출 ₩100,000+ (마감 2026-08-22). 광고비 없이 오가닉.**
- 중간 목표: X 팔로워 100명 → 그다음 매출 스프린트.

## 현재 상태 (2026-07-24 새벽 기준)

- **페르소나**: 후쿠마루(ふくまる), 검은 고양이 츤데레. 계정 `@fukumaru_tarot`.
  프로필 사진은 흰 고양이 → "로브를 입은 순간만 검은고양이 복을 빌려온다"는 설정으로 정합성 맞춤.
  가이드: `marketing/persona-guide.md`. 옛 `marketing/x-queue-ja.md`(즉시 판매용)는 보류 상태.
- **콘텐츠 큐**: `marketing/fukumaru-queue-ja.md` — 22개 중 **1~6번 게시 완료**(7/21~22, 전부 브라우저 수동 게시), 7번부터 대기.
- **성과**: 팔로워 0, 전 포스트 좋아요·댓글·리포스트 0, 조회수 1~6. (7/22 저녁 기준)
- **게시 방식 (7/24 결정): 브라우저 수동 게시로 확정.** X API는 무료 티어가 없고 선불 크레딧($5 최소)만 가능한데, 사용자가 "구매 발생 전 비용 부담스럽다"며 크레딧 충전 거절. API 파이프라인(`fukumaru-post.yml`)은 **비활성화**해둠(402로 매 슬롯 실패하며 알림 스팸만 만들어서). 크레딧 충전하게 되면 `gh workflow enable fukumaru-post.yml`로 재가동 — 코드는 그대로 살아 있음.
- **노트북 데드맨 스위치 (7/24 가동 중, 장애·복구 양방향 테스트 완료)**: 노트북이 죽으면 수동 게시 세션도 죽으므로 감시 장치 추가.
  - 노트북 launchd 에이전트(`~/Library/LaunchAgents/com.rainbow-tarot.fukumaru-heartbeat.plist`)가 10분마다 `scripts/fukumaru/heartbeat.sh` 실행 → GitHub 저장소 변수 `FUKUMARU_HEARTBEAT`에 타임스탬프.
  - GitHub Actions `.github/workflows/fukumaru-watchdog.yml`(30분마다)가 하트비트 35분 이상 끊기면 **Slack 알림**(`SLACK_WEBHOOK_URL` 시크릿, 등록 완료), 복구 시 복구 알림. 장애 상태는 `fukumaru-watchdog` 라벨 이슈로 관리(열림=장애 중, 자동 닫힘=복구; GITHUB_TOKEN이 저장소 변수 쓰기가 안 돼서 이슈 방식 채택). 노트북 잠자기도 "죽음"으로 판정됨 — 알림 잦으면 threshold(2100초) 상향.
- **Netlify 빌드 절약 규칙 (7/24)**: `netlify.toml`의 `ignore` — `.github/`·`marketing/`·`log/`·`scripts/fukumaru/`·`HANDOFF.md`만 바뀐 push는 빌드 스킵. 사이트 파일이 하나라도 섞이면 정상 빌드.

## 🚨 지금 막힌 것

1. ~~X API 402~~ → **수동 게시로 우회하기로 결정(7/24).** 상세는 위 "게시 방식" 참고.
2. Gemini 이미지 생성 429 할당량 초과(무료 티어 한도 0, 네오라 봇과 키 공유 탓) — 수동 게시 체제에서는 어차피 안 씀. 보류.
3. **수동 게시의 구조적 공백**: 세션을 연 사람이 있어야만 게시됨 → 새벽·밤 슬롯 계속 누락(7/23~24, 7/25 저녁, 8·9번 미게시). 자동화 시도 결과(아래) 모두 제약과 충돌.

## ⭐ 게시 방식 = X 네이티브 예약 발행 (2026-07-26 채택, 삼중 조건 충족)

**돈 0 + Claude 사용량 0(발행 시점) + 세션 없이 자동** — 이 세 조건을 동시에 만족하는 유일한 방법. 세션이 열려 있을 때 며칠치를 한 번에 예약해두면 X 서버가 알아서 발행한다. 노트북 꺼져 있어도, 세션 닫혀 있어도 발행됨. 실전 검증 완료(#8·9·10이 7/26 예약대로 정상 발행, 팔로워 0→2).

**방법**: 작성창(`x.com/compose/post`)에서 글 입력 → 툴바 **시계 아이콘(예약)** 클릭 → 다이얼로그에서 Month/Day/Hour/Minute/AM·PM 설정(시간대는 이미 KST) → **Confirm** → **Schedule** 버튼. 예약 목록 확인: `x.com/compose/post/unsent/scheduled`.

**슬롯 표준(한 번에 잡히는 시각으로 통일)**: 아침 09:09 / 오후 15:23 / 저녁 20:41 KST. (시(hour) 셀렉트에서 "1"·"2"는 10·11·12로 사이클링되니 3~9시만 한 번에 잡힘.)

**⚠️ 실패 모드(반드시 지킬 것)**:
- 작성창 진입 직후 첫 `type`은 자주 유실됨 → 입력 후 스크린샷으로 텍스트 확인, 비어 있으면 재입력.
- **Escape 금지** — 모달 전체가 닫혀 입력 유실됨.
- **Schedule 버튼은 한 번만 클릭하고 /home 이동으로 확정 확인.** 선제적 더블클릭 금지 — 두 번째 클릭이 타임라인 트윗을 열면서 예약 POST를 취소시킴(#14·#16이 이렇게 유실됨).
- 창 크기가 1483/1512/1536으로 계속 바뀌어 좌표가 밀림 → 매 단계 스크린샷으로 위치 확인.
- **예약 후 반드시 `x.com/compose/post/unsent/scheduled`에서 실제 목록 대조.** 확정 배너를 봤어도 목록에 없으면 실패한 것.

## 자동게시 대안 기록 (재테스트 금지 — 전부 제약과 충돌)

- **X API 파이프라인**: 무료 티어 없음, 선불 크레딧 $5 최소. 사용자 비용 거절(2026-07-24).
- **headless `claude -p` + 브라우저 (2026-07-26 Opus 테스트)**: 오르카 내장 브라우저만 제어 가능(실제 크롬 확장 못 잡음), 그 브라우저는 X 로그아웃 상태 + 게시마다 Opus 사용량 소모. 비채택.
- **CronCreate(세션 크론)**: 세션 종료 시 소멸 → 무의미.

## ✅ 매 세션 루틴 (사용자 지시 7/25: 여기 기록된 일은 반드시 실행할 것, 허락 재요청 불필요)

게시 세션마다 아래를 전부 돈다. 스킵하면 사유를 이 파일에 남긴다.

1. **게시 = X 네이티브 예약 발행** (위 ⭐섹션 참고). 세션 열릴 때 며칠치를 미리 예약. 예약 후 큐에 `[x]`+예약시각 기록, 반드시 예약목록 대조. (수동 즉시 게시는 예외적으로만.)
   Slack 알림 워크플로(`fukumaru-post-notify.yml`)는 즉시 게시용이었음 — 예약 발행은 세션 없이 나가므로 알림 안 뜸(트레이드오프, 감수).
2. **소통 라운드** (사전 승인됨, 7/25): 라이브 검색(`#猫のいる暮らし` `#犬のいる暮らし` 등 Latest 탭)에서 몇 분 내 올라온 글 3~5개에 좋아요+짧은 자연스러운 답글, 맞는 계정은 가끔 팔로우. 팔로우 목록 뒤지지 말 것(죽은 계정 많음). 신규 계정 스팸 필터 주의 — 당분간 세션당 3~5건 유지.
3. **알림 확인**: 우리 답글에 온 반응(좋아요·답글·팔로우)에 다시 반응. 팔로워 전환의 핵심 경로.
4. **기록**: `log/team-lead-journal.md` 일지 + 큐 파일 + 이 HANDOFF 갱신 → 로컬 커밋(main 포인터 이동, push는 사용자 승인 시).

## 대기 중 일회성 작업

- **재예약 필요(7/26 예약 시 UI 유실)**: #14 → 7/27 20:41, #16 → 7/28 15:23. 재예약 전 반드시 예약목록에서 중복 없는지 확인.
- **현재 예약 상태(2026-07-27 새벽 확인)**: 발행완료 #8·9·10(7/26). 예약됨 #11(7/27 09:09)·#12(7/27 15:23)·#15(7/28 09:09)·#17(7/28 20:41). 큐는 18번까지 있고 19~22는 다음 주차용.
- 7/28 이후 게시 슬롯: 큐 #18(The Moon)부터 이어서 예약.
- 계정 2주차(~8/4)쯤 소통량 세션당 5~8건으로 증량 검토.
- 8월 초: 리딩 CTA 포스트(큐 15번 계열)로 매출 전환 시작 — 8/22 ₩100k 목표 역산. (#15는 7/28 예약 완료.)

## 대기 중 (막힌 것 아님, 미션 외)

- 해외 결제 수단 확장: Lemon Squeezy 답변 대기(Paddle은 점술 카테고리 거절). 현재 PayPal(¥400)은 살아 있음.
- 한국어 나이스페이 실결제 셀프 테스트(₩1,500) — 우선순위 낮음.
- Supabase RLS 때문에 anon 세션에서 `reading_history` 조회 불가 → 결제 0건이 진짜 0건인지 서버 함수 경유로 확인 필요.

## 운영 규칙 (요약 — 상세는 메모리)

- **git push는 사용자 요청 시에만.** 로컬 커밋까지만 하고 push는 사용자에게 맡길 것.
- Netlify 배포 크레딧 아끼기 — 변경사항 모아서 한 번에.
- 일본어 텍스트는 항상 한국어 번역 병기 (사용자가 일본어 못 읽음).
- 유니코드 이모지 최소화, 가오모지 사용. 답글은 짧고 평범하게(따옴표 스타일 금지).
- 일지는 윤자동식 "~다" 건조체.
- 톤/카피 같은 주관적 결정은 객관식으로 좁히기 전에 사용자가 자유롭게 말할 여지부터 줄 것.
- **브라우저 자동화(Claude in Chrome) 사용량 최소화 원칙 (사용자 지시 7/25): 효과·정확도를 유지하는 선에서 항상 사용량이 가장 적은 방식을 먼저 고른다.**
  - 스크린샷(이미지 토큰)보다 `get_page_text`/`read_page`/`find`(텍스트) 우선. 스크린샷은 시각 확인이 진짜 필요할 때만(레이아웃 확인, 클릭 좌표 잡기 등).
  - 액션을 개별 호출로 쪼개지 말고 batch로 묶어서 왕복 줄이기.
  - 게시·소통 세션은 짧게 끊기 — 스크린샷이 쌓일수록 컨텍스트·비용이 커짐.
  - **단, 결과를 확실히 확인해야 하는 지점(게시 성공 여부, 폼 제출 결과)에서 확인을 생략해 정확도를 깎지 않는다** — 최소화가 검증 스킵으로 이어지면 안 됨.

## 파일 위치

- 콘텐츠 큐: `marketing/fukumaru-queue-ja.md`
- 페르소나 가이드: `marketing/persona-guide.md`
- 파이프라인: `.github/workflows/fukumaru-post.yml`, `scripts/fukumaru/`, `marketing/state/fukumaru-schedule.json`
- 일지: `log/team-lead-journal.md` (팀장 일지), `log/YYYY-MM-DD.md` (일별)
- 이 파일: 저장소 루트 `HANDOFF.md`

## 매일 보고 형식

`방문자 수 / 유입 채널 / 결제 시도 / 결제 완료 / 오늘 한 일 / 내일 할 일 / 필요한 결정`
