# 인수인계 — 일본 매출 미션 (다른 터미널/세션에서 이어가기)

이 파일을 새 세션의 클로드 코드에 "HANDOFF.md 읽고 이어서 진행해줘"라고 붙여넣으면 됩니다.
(작업 디렉토리는 `~/rainbow-tarot`, 브랜치는 보통 `dev`에서 작업 후 `main`으로 머지.)

세션이 바뀌면 대화 맥락은 안 이어지지만, **메모리(`~/.claude/projects/.../memory/`)에 저장된 결정·피드백은 자동으로 불러와집니다.** 이 파일은 그걸 보완하는 상세 스냅샷입니다.

---

## 현재 미션

**한 달 내(2026-08-22까지) 일본에서 신규 매출 ₩100,000 이상.**

- 이전엔 "팔로워 100명까지는 판매 언급 자제"였는데 2026-07-22에 폐기함 — 사용자가 "너무 소극적인 액션만 하고 있는듯"이라고 직접 지적. 지금은 CTA를 정기적으로 노출하고, 오가닉 소통량도 적극적으로 늘리는 방향.
- 광고비: **아직 사용 안 하기로 함**(2026-07-24 기준). 다시 물어봐야 할 수도 있음.

## 팀 구조 ("직원" 컨셉 — 사용자가 원한 방식)

- **팀장 = Fable 5 (나, 메인 세션)** — 전체 총괄, 외부 게시/결제 변경 등 리스크 있는 액션은 항상 여기서 승인 게이트 거침. 매일 `log/team-lead-journal.md`에 일지 씀(오늘 한 일 / 오늘 틀린 일, 윤자동 스레드 톤 — 딱딱한 보고체 아니고 "~다" 짧은 문장체).
- **마케팅1 (콘텐츠 제작/포스팅)** — Agent 툴, subagent_type `fast-worker`. 페르소나 확립 초반엔 model `opus`, 안정화 후 `sonnet`. 지금까지: 후쿠마루 페르소나 1주차 콘텐츠 21개(JP+KR) 초안 작성, 이모지→가오모지 교체, 가격 배지 UI 롤아웃 등을 위임했었음.
- **마케팅2 (댓글 대응)** — 아직 실제로 가동 안 함(설계만 돼있음). model `haiku` 기본, 애매하거나 부정적인 댓글은 sonnet/팀장으로 에스컬레이션.
- 모든 콘텐츠/댓글은 **일본 감수성 체크리스트**(`marketing/persona-guide.md` 참고: 과장광고 금지, 펫로스 톤 주의, 감정 콘텐츠 직후 하드셀 금지, 마스코트 상표 충돌 확인, 문화적 금기)를 통과해야 함.

## 캐릭터: 후쿠마루 (ふくまる)

- 검은고양이 컨셉(福=복), 츤데레 성격, 말투는 にゃ/にゃん 어미(한국식 "냥냥(대사)" 형식 안 씀), 1인칭 僕(ぼく).
- **프로필 사진은 실제로는 흰 고양이**(로브 입고 타로 보는 일러스트) — "로브를 입은 순간만 검은고양이 복을 빌린다"는 설정으로 정합성 맞춤.
- 이모지 최소화, 가오모지(顔文字) 사용: (´˘\`) ฅ^•ω•^ฅ (ﾉωﾉ) ( ˘ω˘) (-ω-。) (｡•ᴗ•｡)✧ — 유니코드 이모지 많이 쓰면 AI 티 난다는 사용자 피드백 반영.
- 답글/소통 시엔 이모지도 최소, 「」/"" 따옴표 인용 문체도 쓰지 않음(부자연스러움) — 짧고 진짜 반응처럼.

## X 계정 현황

- 핸들: **@fukumaru_tarot** (사용자 개인 계정 @onione2173과 별개, 새로 만든 계정)
- 콘텐츠 큐: `marketing/fukumaru-queue-ja.md` — 21개 중 1~6번 게시 완료(날짜/URL 기록돼있음), 22번은 예비 FAQ(수동 트리거 전용, 자동 게시 로직에서 제외)
- 팔로잉 20+개 계정(라이브 해시태그 검색으로 신선한 글 찾아서 팔로우+좋아요+짧은 댓글 하는 방식이 효과적이었음 — `#猫のいる暮らし` `#うちの子自慢` `#猫好きさんと繋がりたい` 등)
- 팔로워는 아직 거의 없음(1명, 그나마 콘텐츠 보고 온 건 아닌 걸로 보임) — 정직하게 느린 상태
- 경쟁 계정 조사함: **@nekoyomi_yamaz** — 고양이 탐정 부부가 코코나라에서 타로 리딩 파는 계정. 46팔로워로도 실제 12건 판매(₩14,000 상당) 달성 — 팔로워 수보다 해시태그 도달+신뢰신호(리뷰)가 더 중요하다는 시사점.
- **코코나라 진출은 포기함**: 일본 전화번호·신분증·은행계좌 필요해서 한국 거주자는 정산(현금화)이 사실상 안 됨. 대신 우리 사이트 직결제 채널에 집중.

## 자동 게시 파이프라인 (GitHub Actions — 세션 없이도 작동)

- `.github/workflows/fukumaru-post.yml` — 매시간 실행, `main` 브랜치 기준으로만 스케줄 트리거됨(그래서 dev 작업 후 반드시 main으로 머지+push 필요)
- `scripts/fukumaru/` — post_next.py(진입점), schedule_state.py(하루 랜덤 시각 3~5개 관리, 상태는 `marketing/state/fukumaru-schedule.json`에 커밋), x_client.py(X API v2 OAuth1.0a), gemini_image.py(이미지 생성, 참조 이미지로 캐릭터 일관성 유지), queue_parser.py
- GitHub Secrets 등록 완료: `GEMINI_API_KEY`(네오라 프로젝트 키 재사용), `X_API_KEY`, `X_API_SECRET`, `X_ACCESS_TOKEN`, `X_ACCESS_TOKEN_SECRET` — 전부 실제 호출로 인증 확인함(200 OK)
- **2026-07-22/23에 실제로 라이브 확인 완료** — 세션 없이 GitHub 서버에서 스스로 스케줄 판단하고 상태 파일 커밋하는 것까지 확인함. 더 이상 이론이 아니라 실제로 돌아가는 상태.
- X 앱은 "Pay Per Use" 요금제, 현재 잔액 $0/결제수단 없음/지출한도 무제한 — 카드 없어서 지금은 과금 자체가 안 되는 구조. 사용자가 지출한도 설정은 나중으로 미룸.
- Gemini 이미지 생성은 네오라 봇이랑 키 공유해서 **할당량(429) 이슈가 있을 수 있음** — 실패해도 텍스트만으로 폴백되게 설계돼있음.

## 가격 (2026-07-24 변경)

"오픈 이벤트 50% 할인" 프레임으로 전부 통일:
| 시장 | 정가(취소선 표시) | 실제가 |
|---|---|---|
| 한국 | ~~₩3,000~~ | ₩1,500 (안 바뀜) |
| 일본 | ~~¥600~~ | **¥300** (¥400에서 인하 — 결제 검증 로직도 동기화함) |
| 영어권 | ~~$5.99~~ | $2.99 (안 바뀜) |

일본 가격 변경은 `ja/deep-reading/index.html` + `netlify/functions/paypal-order.js` + `netlify/functions/deep-tarot-background.js` 세 곳이 정확히 일치해야 결제가 안 깨짐(과거 "캡처 금액 vs 원 주문 금액 불일치" 사고 있었음, 재발 주의).

## 계획됐지만 아직 구현 안 한 것

- **후기 제출 기능**: 결제 완료 후 결과 페이지에서 별점(필수)+자유 텍스트(선택, "혹시 더 하고싶은 말씀이...") 받고, `approved=false`로 저장 → 사람이 검수 후 공개. **가짜 후기는 절대 금지**(경품표시법 리스크 + 브랜드 신뢰 붕괴 위험) — 실제 후기 쌓일 때까지 위젯은 비워두거나 숨김. Supabase `reviews` 테이블 설계는 돼있음(위 대화 참고), 아직 코드로 안 옮김.

## 알아둬야 할 함정들

- **X 신규 계정 "Unlock more on X" 게이트**: 초반엔 검색 노출·DM 등이 제한됨, 시간 지나야 풀림.
- **280자 제한 + 이모지 가중치**: 결합 이모지(🐈‍⬛ 등)가 일반 글자보다 많이 카운트됨. 게시 직전 카운터 확인 습관.
- **X 웹 Post 버튼이 가끔 두 번 눌러야 반영됨** — 클릭 후 스크린샷으로 실제 게시됐는지("Your post was sent" 토스트/포스트 개수 증가) 꼭 확인.
- **file_upload 브라우저 툴이 로컬 파일 경로를 못 받는 버전 이슈** — 이미지 업로드는 사용자가 직접 해야 함.
- **`gh` 워크플로 파일 push엔 `workflow` OAuth 스코프 필요** — 없으면 `gh auth refresh -h github.com -s workflow` (사용자가 device code 승인 필요) 후 `gh auth setup-git` 까지 해야 실제로 반영됨.
- **세션 크론(CronCreate)/백그라운드 서브에이전트는 터미널 끄면 같이 죽음** — GitHub Actions 파이프라인만 완전히 독립적. 새 세션 열 때마다 "오늘 크론 걸렸는지"부터 확인하는 습관 필요(단, 이제 GitHub Actions가 있어서 예전만큼 중요하지 않음).
- **push는 항상 사용자 확인 받고 진행** — 로컬 커밋까지는 알아서 해도 되지만 push/머지는 매번 여쭤봄.

## 파일 위치 정리

- 페르소나 가이드: `marketing/persona-guide.md`
- 콘텐츠 큐: `marketing/fukumaru-queue-ja.md`
- 팀장 일지: `log/team-lead-journal.md`
- 일일 운영 보고(방문자/전환): `log/YYYY-MM-DD.md`
- 자동 게시 파이프라인: `scripts/fukumaru/`, `.github/workflows/fukumaru-post.yml`, 상태 파일 `marketing/state/fukumaru-schedule.json`
- 이 인수인계 파일: `~/rainbow-tarot/HANDOFF.md` (진행되는 대로 계속 갱신할 것)

## 매일 보고 형식
`방문자 수 / 유입 채널 / 결제 시도 / 결제 완료 / 오늘 한 일 / 내일 할 일 / 필요한 결정`
